import React from "react";
import PrimaryButton from "@/Components/PrimaryButton.jsx";

const MarkAccountDefault = ({ account }) => {
    return (
        <PrimaryButton>

        </PrimaryButton>
    );
}

export default MarkAccountDefault;
